<odoo>
    <!-- Campaign list View with Button -->
    <record id="view_facebook_campaign_list" model="ir.ui.view">
        <field name="name">crm.facebook.campaign.list</field>
        <field name="model">crm.facebook.campaign</field>
        <field name="arch" type="xml">
            <list string="Facebook Campaigns" create="false" delete="false">
                <field name="name"/>
                <field name="fb_form_id"/>
                <field name="status"/>
                <field name="page_id"/>
                <button name="action_view_leadforms"
                        type="object"
                        string="View Lead Forms"
                        class="btn-secondary"/>
            </list>
        </field>
    </record>

    <!-- Lead Form list View -->
    <record id="view_facebook_leadform_list" model="ir.ui.view">
        <field name="name">crm.facebook.leadform.list</field>
        <field name="model">crm.facebook.leadform</field>
        <field name="arch" type="xml">
            <list string="Facebook Lead Forms" create="false" delete="false">
                <field name="name"/>
                <field name="form_id"/>
                <field name="campaign_id"/>
                <field name="page_id"/>
                <field name="leads_count"/>
               
            </list>
        </field>
    </record>

    <!-- Lead Form Form View -->
    <record id="view_facebook_leadform_form" model="ir.ui.view">
        <field name="name">crm.facebook.leadform.form</field>
        <field name="model">crm.facebook.leadform</field>
        <field name="arch" type="xml">
            <form string="Facebook Lead Form" create="false" delete="false">
                <header>
                    <button name="action_import_fields"
                            type="object"
                            string="Import Fields"
                            class="btn-primary"/>
                    <button name="action_fetch_leads"
                            type="object"
                            string="Fetch Lead"
                            class="btn-primary"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name" string="Form Name" readonly="1"/>
                            <field name="form_id" string="Form Id" readonly="1"/>
                            <field name="page_id" string="Facebook Page" readonly="1"/>
                        </group>
                        <group>
                            <field name="instance_id" string="Facebook Instance" readonly="1"/>
                            <field name="pagination_size" string="Pagination Size"/>
                            <field name="last_sync_date" string="Last Sync Date"/>
                            <field name="active_scheduler" string="Active Scheduler" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <group string="FIELDS MAPPING">
                        <field name="mapping_line_ids" nolabel="1" colspan="2">
                            <list editable="bottom">
                                <field name="odoo_field_id" string="Odoo Fields Label"/>
                                <field name="odoo_field_name" string="Odoo Fields" readonly="1" force_save="1"/>
                                <field name="odoo_field_type" string="Odoo Fields Type" readonly="1" force_save="1"/>
                                <field name="fb_field_label" string="Facebook Fields Label"/>
                                <field name="fb_field_name" string="Facebook Fields"/>
                                <field name="fb_field_type" string="Facebook Fields Type"/>
                                <field name="description" string="Description"/>
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Lead Form Action -->
    <record id="action_facebook_leadform" model="ir.actions.act_window">
        <field name="name">Lead Forms</field>
        <field name="res_model">crm.facebook.leadform</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Lead Form Search View -->
    <record id="view_facebook_leadform_search" model="ir.ui.view">
        <field name="name">crm.facebook.leadform.search</field>
        <field name="model">crm.facebook.leadform</field>
        <field name="arch" type="xml">
            <search string="Facebook Lead Forms">
                <field name="name"/>
                <field name="form_id"/>
                <field name="page_id"/>
                <separator/>
                <filter name="group_by_page" string="Facebook Page" context="{'group_by': 'page_id'}"/>
                <filter name="group_by_campaign" string="Campaign" context="{'group_by': 'campaign_id'}"/>
            </search>
        </field>
    </record>
    <!-- Inherit CRM Lead Form View to add Facebook fields -->
    <record id="crm_lead_view_form_inherit_facebook" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.facebook</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Facebook Lead Info" name="facebook_info" invisible="not fb_lead_id">
                    <group>
                        <field name="fb_lead_id" readonly="1"/>
                        <field name="fb_form_id" readonly="1"/>
                        <field name="fb_campaign_id" readonly="1"/>
                        <field name="raw_data" readonly="1"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Facebook Leads Action (filtered CRM leads) -->
    <record id="action_facebook_leads" model="ir.actions.act_window">
        <field name="name">Facebook Leads</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('fb_lead_id', '!=', False)]</field>
        <field name="context">{'default_type': 'lead'}</field>
    </record>

    <!-- Hide Salesperson/Assignee column in List View -->
    <record id="crm_lead_view_list_inherit_hide_assignee" model="ir.ui.view">
        <field name="name">crm.lead.list.inherit.hide.assignee</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_leads"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='user_id']" position="attributes">
                <attribute name="column_invisible">1</attribute>
            </xpath>
        </field>
    </record>
</odoo>
